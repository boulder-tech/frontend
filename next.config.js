/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false, //this is set by default true, causing renders to run twice which is annoying. I prefer set this to false
};

module.exports = nextConfig;
